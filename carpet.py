import argparse

if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="CLI interface for carpet")
    parser.add_argument("A cargo-like utility to manage virtual environments "
                        "and dependencies in Python, in a natural and logical"
                        "manner")
    
